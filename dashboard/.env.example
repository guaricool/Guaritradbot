# Sprint 46B — the trading dashboard.
#
# Where the bot's HTTP API lives. Set this to the public URL that proxies
# to the bot's port 8080 on the VPS (e.g. https://bot-api.guaritradbot.com).
# In local dev, the bot runs on http://localhost:8080 and so does this dashboard.
NEXT_PUBLIC_API_URL=http://localhost:8080
