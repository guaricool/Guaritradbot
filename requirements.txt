pandas==2.2.2
numpy==1.26.4
PyYAML==6.0.1
ccxt==4.3.43
python-dotenv==1.0.1
schedule==1.2.2
streamlit==1.36.0
requests==2.32.3
# yfinance >= 1.0: 0.2.40 (Feb 2024) está ROTO — Yahoo cambió la API y devuelve HTML en vez de JSON.
# 1.x reescrito por completo, usa curl_cffi internamente con impersonation Chrome.
yfinance>=1.0,<2.0
# curl_cffi: fallback explícito para cuando Yahoo anti-botea al server-pool del VPS.
curl_cffi>=0.7,<1.0
ta==0.11.0
# Sprint 19: ML pipeline (LogisticRegression baseline + scaler)
scikit-learn>=1.3.0
scipy>=1.10.0
plotly==5.22.0
# streamlit-autorefresh: non-blocking page reload — avoids the 15s sleep that
# `time.sleep + st.rerun()` causes on the FIRST render of the dashboard.
streamlit-autorefresh==1.0.1
# Sprint 36: Alpaca Markets REST API client (equities/ETFs routing).
# Imported as `alpaca_trade_api` even though the pip package is `alpaca-py`.
alpaca-py>=0.0.4,<1.0
